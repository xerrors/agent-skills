# 默认音频

`default.mp3` 是生成项目默认复制到 `assets/audios/default.mp3` 的背景音乐。

当前文件由用户指定并从以下本地素材复制：

`joyinsound-corporate-background-music-for-production-500250.mp3`

默认音频只是占位与效果预览素材，可以在工作台中上传替换。正式发布前应确认素材的授权范围、署名要求和分发权限。
